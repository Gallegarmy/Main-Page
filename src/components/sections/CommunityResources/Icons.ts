import { IconDefinition, faGithub } from '@fortawesome/free-brands-svg-icons';
import { faBeerMugEmpty, faClock, faHammer } from '@fortawesome/free-solid-svg-icons';

export const communityIconMap: Record<string, IconDefinition> = {
  beer: faBeerMugEmpty,
  github: faGithub,
  hammer: faHammer,
  clock: faClock,
};
